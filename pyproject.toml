[project]
name = "keystroke-count"
version = "0.1.0"
description = "CLI keystroke counter for macOS"
requires-python = ">=3.10"
dependencies = [
    "pynput>=1.7.6",
    "pyobjc-framework-Quartz>=10.0",
]

[project.scripts]
keystroke-count = "keystroke_count.cli:main"
